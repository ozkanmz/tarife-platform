# tarife-platform — yayın reposu .gitignore
# Bu repo SADECE canlı siteyi barındırır (index.html + og-image.svg).
# Python kodu, veri arşivleri, kimlik bilgileri BURADA YAŞAMAZ — onlar
# ayrı kod reposundadır (ozkanmz/Enerji-Portali). Bu yüzden buradaki
# kurallar bilinçli olarak minimaldir.

# --- Editör/OS kalıntıları ---
.vscode/
.idea/
Thumbs.db
.DS_Store
*.swp
